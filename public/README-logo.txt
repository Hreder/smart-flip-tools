Save your Smart Flip Tools logo in THIS folder as exactly:

    logo.png

Vite serves it at /logo.png and the site picks it up automatically
(header + footer). Until the file exists, the site shows a recreated
SVG version of the logo as a fallback. No rebuild needed — just drop
the file in and refresh the browser.
